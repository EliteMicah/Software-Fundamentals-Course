// Lesson 1
//const arr = new Array(20, 20, 20);
// const arr = [20, 20, 20];

/*
// Lesson 2
const arr = [];

const name1 = prompt("Enter Name");
const age = prompt("Enter Age");
const hairColor = prompt("Enter Hair Color");
// const arr = [name1, age, hairColor];

if (name1 !== "") {
  arr.push(name1);
}
if (age !== "") {
  arr.push(age);
}
if (hairColor !== "") {
  arr.push(hairColor);
}

const del = prompt("Delete last Element?");

if (del == "yes") {
  arr.pop();
}
alert(arr);
*/

/*
// Lesson 3
const arr = [];
const arr2 = [1, 2, 3];
const arr3 = [3, 4, 5, ["Hello"]]; // 2 dimensional Array

arr.push(arr2);
arr.push(arr3);


console.log(arr[1][3][0]); // logs "Hello"
*/

// Lesson 4
const arr = [45, 23, 33, -8, 7];
//const stringArr = arr.toString();
// arr.unshift(-1); places at the beginning
// arr.shift(5); places at the end

// arr.sort() sorts by 1 char at a time
// arr.sort((a, b) => a - b); // Sort by ascending (custom function sort)
// arr.sort((a, b) => b - a); // Sort by descending (custom function sort)

// const arr2 = ["Hello", "world"];
//const arr3 = arr.concat(arr2); // Join together the arrays

// const removed = arr.splice(2, 3); removes 3 elements starting at index 2
// const str = arr.join(" | "); // Customizes whats placed in between the elements
// const sl = arr.slice(1, 4); // Goes up to index 4 not including 4

console.log(sl);
